---
# - name: Simple task running on all hosts 
#   hosts: all
#   tasks:
#     - debug:
#         msg: 'Running on all hosts'
#     - shell: hostname -f

- name: Configuring Server Frontend
  hosts: soufe1
  vars_files:
    - vars/main.yml
  roles:
    - sou_podman
 
- name: Configuring Server Backend
  hosts: soube2
  vars_files:
    - vars/main.yml
  roles:
    - sou_podman

